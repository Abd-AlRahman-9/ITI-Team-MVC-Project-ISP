@model WebApplication1.ViewModels.CustomerViewModel

@{
    SelectList offer = new SelectList(ViewBag.offerlist, "Id", "Name");
    SelectList package = new SelectList(ViewBag.packagelist, "Id", "Name");
    SelectList service = new SelectList(ViewBag.servicelist, "Id", "Name");
}

<h1>Edit</h1>

<h4>CustomerViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Adress" class="control-label"></label>
                <input asp-for="Adress" class="form-control" />
                <span asp-validation-for="Adress" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone" class="control-label"></label>
                <input asp-for="Phone" class="form-control" />
                <span asp-validation-for="Phone" class="text-danger"></span>
            </div>
            <div>
                <label asp-for="PackageId"></label>

                <select asp-for="PackageId" asp-items="package" class="form form-control">
                    <option value="0"> --Select -- </option>
                </select>
                <span asp-validation-for="PackageId" class="text-danger"></span>
            </div>

            <div>
                <label asp-for="ProviderId"></label>

                <select asp-for="ProviderId" asp-items="service" class="form form-control">
                    <option value="0"> --Select -- </option>
                </select>
                <span asp-validation-for="ProviderId" class="text-danger"></span>
            </div>

            <div>
                <label asp-for="OfferId"></label>

                <select asp-for="OfferId" asp-items="offer" class="form form-control">
                    <option value="0"> --Select -- </option>
                </select>
                <span asp-validation-for="OfferId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
